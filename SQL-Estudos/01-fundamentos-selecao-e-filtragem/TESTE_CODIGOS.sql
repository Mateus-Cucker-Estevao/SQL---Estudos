-- CHAMANDO O BANCO QUE VAI SER USADO

USE PRIMEIRO_BD

-- SELECIONANDO APENAS UMA COLUNA DE UAM TABELA
-- COM CONDIÇÕES (WHERE)

SELECT
	EMAIL,
	NOME
FROM 
	CLIENTE
WHERE 
	ID_CLIENTE = 3

